def palindrome_identifier(word):
    word = word.lower().strip()
    reversed = word[::-1]
    if word == reversed:
        is_palindrome = True
    else:
        is_palindrome = False

    return is_palindrome
