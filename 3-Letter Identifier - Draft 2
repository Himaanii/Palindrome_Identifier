print("Enter a 3 letter word.")
a = []
r = []

word = input() 

for i in range(3):
  a.append(word[i].upper())  
  r.append(word[i].upper())

#print(a)
#print(r)
  
r.reverse()

if a==r:
  print(word, "is a palindrome.")
else:
  print("Sorry", word, "is not a palindrome")
 
